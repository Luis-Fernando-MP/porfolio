---
id: '20d98397-f7fa-80ed-ae15-d5e783a9ee85'
title: 'DPS - UI/UX de navegación'
reading_time: 1
words: 128
priority: 'Alta'
team: 'DPS'
progress: 1
banner: '/content/projects/20d98397-f7fa-80ed-ae15-d5e783a9ee85/banner.webp'
banner_width: 1500
banner_height: 2042
aspectRatio: 0.7339449541284404
image_hash: 'VThLS1d1flhIc3NFMDZvT25DajA4d2F5P0lvZj9AYkhwQmcx'
image_blur: ''
thumb: '/content/projects/20d98397-f7fa-80ed-ae15-d5e783a9ee85/thumb.webp'
thumb_width: 400
thumb_height: 545
created_time: '2025-06-09T03:22:00.000Z'
last_edited_time: '2025-07-25T13:40:00.000Z'
tags: []


---

<h2 className='notion-heading_2 notion-color-default'>Enlaces de interés:</h2>
<p>hola como estas</p>
```bash
#!/usr/bin/env bash
# Inicio del script Bash
echo "Inicio del script"

# Bucle para imprimir líneas numeradas del 1 al 10
for i in {1..10}; do
  echo "Línea \$i"
done

# Crear un directorio llamado proyecto_test
mkdir proyecto_test

# Cambiar al directorio proyecto_test
cd proyecto_test

# Crear cinco archivos de texto
touch archivo{1..5}.txt

# Escribir en archivo1.txt
echo "Escribiendo en archivo1.txt" > archivo1.txt

# Mostrar el contenido de archivo1.txt
cat archivo1.txt

# Eliminar archivo2.txt
rm archivo2.txt

# Verificar si archivo3.txt existe
if [ -f archivo3.txt ]; then
  echo "archivo3 existe"
fi

# Fin del script
echo "Fin del script"
```

```python
def saludo(nombre):
    # Función para saludar a una persona
    print(f"Hola, {nombre}")

# Iterar sobre una lista de usuarios y saludar a cada uno
for usuario in ["Luis", "Ana", "María"]:
    saludo(usuario)

# Crear una lista de números del 0 al 4
numeros = list(range(5))

# Crear una lista de cuadrados de los números
cuadrados = [n**2 for n in numeros]

# Imprimir números y sus cuadrados
for n, c in zip(numeros, cuadrados):
    print(n, c)

# Imprimir la fecha actual
import datetime
print("Fecha:", datetime.date.today())
```

```javascript
// Función para saludar
function saludo(nombre) {
  console.log('Hola, ' + nombre)
}

// Saludar a cada nombre en la lista
;['Luis', 'Ana', 'María'].forEach(saludo)

// Crear un array de números del 0 al 4
let nums = [...Array(5).keys()]

// Crear un array de cuadrados de los números
let cuadrados = nums.map(n => n * n)

// Imprimir números y sus cuadrados
for (let i = 0; i < nums.length; i++) {
  console.log(nums[i], cuadrados[i])
}

// Imprimir la fecha actual
console.log('Fecha:', new Date().toLocaleDateString())
```

```typescript
// Definir un tipo Persona
type Persona = { nombre: string; edad: number }

// Crear un array de personas
const personas: Persona[] = [
  { nombre: 'Luis', edad: 21 },
  { nombre: 'Ana', edad: 25 },
  { nombre: 'María', edad: 30 }
]

// Saludar a cada persona
personas.forEach(p => console.log(`Hola, ${p.nombre}`))

// Crear un array de números
const nums: number[] = [1, 2, 3, 4, 5]

// Crear un array de cuadrados de los números
const cuadrados: number[] = nums.map(n => n ** 2)

// Imprimir los cuadrados
cuadrados.forEach(c => console.log(c))
```

```html
<!DOCTYPE html>
<html lang="es">
  <head>
    <!-- Configuración del documento HTML -->
    <meta charset="UTF-8" />
    <title>Prueba</title>
  </head>
  <body>
    <!-- Encabezado de la página -->
    <header><h1>Hola Mundo</h1></header>
    <!-- Sección principal -->
    <section>
      <p>Ejemplo de HTML multiparámetro</p>
      <!-- Lista de elementos -->
      <ul>
        <li>Item 1</li>
        <li>Item 2</li>
        <li>Item 3</li>
      </ul>
    </section>
    <!-- Pie de página -->
    <footer>Fin del documento</footer>
  </body>
</html>
```

```css
/* Estilos para el cuerpo del documento */
body {
  margin: 0;
  padding: 0;
  font-family: sans-serif;
}

/* Estilos para el encabezado */
header {
  background: #333;
  color: white;
  padding: 1rem;
}

/* Estilos para la sección */
section {
  padding: 2rem;
}

/* Estilos para los elementos de la lista */
ul li {
  list-style: none;
  margin: 0.5rem 0;
}

/* Estilos para el pie de página */
footer {
  text-align: center;
  padding: 1rem;
}
```

```json
{
  "proyecto": "Coral",
  "version": "1.0.0",
  "autores": ["Luis", "Ana", "María"],
  "scripts": {
    "dev": "pnpm dev",
    "build": "pnpm build",
    "test": "pnpm test"
  },
  "dependencias": {
    "react": "^18.2.0",
    "typescript": "^4.9.5"
  },
  "config": {
    "port": 4321,
    "env": "development"
  }
}
```

```sql
-- Crear una tabla llamada usuarios
CREATE TABLE usuarios (
  id SERIAL PRIMARY KEY,
  nombre VARCHAR(100),
  edad INT,
  email TEXT
);

-- Insertar datos en la tabla usuarios
INSERT INTO usuarios (nombre, edad, email) VALUES
('Luis', 21, 'luis@mail.com'),
('Ana', 25, 'ana@mail.com'),
('María', 30, 'maria@mail.com');

-- Seleccionar todos los registros de la tabla usuarios
SELECT * FROM usuarios;

-- Actualizar la edad de Luis
UPDATE usuarios SET edad = edad + 1 WHERE nombre = 'Luis';

-- Eliminar usuarios con edad mayor a 28
DELETE FROM usuarios WHERE edad > 28;
```

```go
package main

import (
  "fmt"
  "time"
)

// Función para saludar
func saludo(nombre string) {
  fmt.Println("Hola,", nombre)
}

func main() {
  // Lista de nombres
  nombres := []string{"Luis", "Ana", "María"}

  // Saludar a cada nombre en la lista
  for _, n := range nombres {
    saludo(n)
  }

  // Imprimir números del 1 al 5
  for i := 1; i <= 5; i++ {
    fmt.Println("Número:", i)
  }

  // Imprimir la hora actual
  fmt.Println("Hora actual:", time.Now())
}
```

```ruby
# Función para saludar
def saludo(nombre)
  puts "Hola, #{nombre}"
end

# Saludar a cada nombre en la lista
["Luis", "Ana", "María"].each { |n| saludo(n) }

# Crear un array de números del 1 al 5
numeros = (1..5).to_a

# Imprimir los cuadrados de los números
numeros.each do |n|
  puts "Cuadrado de #{n} = #{n**2}"
end

puts "Fin del script Ruby"
```

```php
<?php
// Función para saludar
function saludo(\$nombre) {
    echo "Hola, \$nombre\n";
}

// Lista de nombres
\$nombres = ["Luis", "Ana", "María"];

// Saludar a cada nombre en la lista
foreach (\$nombres as \$n) {
    saludo(\$n);
}

// Imprimir números del 1 al 5
for (\$i = 1; \$i <= 5; \$i++) {
    echo "Número \$i\n";
}

// Imprimir la fecha actual
date_default_timezone_set('America/Lima');
echo "Fecha: " . date('Y-m-d') . "\n";
?>
```

```java
public class Main {
  // Función para saludar
  public static void saludo(String nombre) {
    System.out.println("Hola, " + nombre);
  }

  public static void main(String[] args) {
    // Lista de nombres
    String[] nombres = {"Luis", "Ana", "María"};

    // Saludar a cada nombre en la lista
    for (String n : nombres) saludo(n);

    // Imprimir números del 1 al 5
    for (int i = 1; i <= 5; i++) {
      System.out.println("Número " + i);
    }

    System.out.println("Fin del programa Java");
  }
}
```

```c
#include <stdio.h>
#include <time.h>

// Función para saludar
void saludo(const char *nombre) {
  printf("Hola, %s\n", nombre);
}

int main() {
  // Lista de nombres
  char *nombres[] = {"Luis", "Ana", "María"};

  // Saludar a cada nombre en la lista
  for (int i = 0; i < 3; i++) saludo(nombres[i]);

  // Imprimir números del 1 al 5
  for (int i = 1; i <= 5; i++) {
    printf("Número %d\n", i);
  }

  // Imprimir la fecha actual
  time_t now = time(NULL);
  printf("Fecha: %s", ctime(&now));
  return 0;
}
```

```cpp
#include <iostream>
#include <vector>
#include <ctime>
using namespace std;

// Función para saludar
void saludo(const string &nombre) {
  cout << "Hola, " << nombre << endl;
}

int main() {
  // Lista de nombres
  vector<string> nombres = {"Luis", "Ana", "María"};

  // Saludar a cada nombre en la lista
  for (auto &n : nombres) saludo(n);

  // Imprimir números del 1 al 5
  for (int i = 1; i <= 5; i++) {
    cout << "Número " << i << endl;
  }

  // Imprimir la fecha actual
  time_t now = time(0);
  cout << "Fecha: " << ctime(&now);
  return 0;
}
```

```rust
use chrono::Local;

// Función para saludar
fn saludo(nombre: &str) {
    println!("Hola, {}", nombre);
}

fn main() {
    // Lista de nombres
    let nombres = ["Luis", "Ana", "María"];

    // Saludar a cada nombre en la lista
    for n in nombres.iter() {
        saludo(n);
    }

    // Imprimir números del 1 al 5
    for i in 1..=5 {
        println!("Número {}", i);
    }

    // Imprimir la fecha actual
    println!("Fecha: {}", Local::now().format("%Y-%m-%d"));
}
```
